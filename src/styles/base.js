/**
 * Base
 */

const fontBase = 3;

module.exports = {
  fontSize: `${fontBase}rem`,
  fontXs: `${fontBase * 0.65}rem`,
  fontSm: `${fontBase * 0.75}rem`,
  fontMd: `${fontBase}`,
  fontLg: `${fontBase * 1.5}rem`,
  fontXl: `${fontBase * 2}rem`,
  font2Xl: `${fontBase * 3}rem`,
};
